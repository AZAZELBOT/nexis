---
title: Matchmaking
description: Queue players and react to match.found.
---

# Matchmaking

## Enqueue

```ts
await client.enqueueMatchmaking('counter_plugin_room', 2)
```

## Dequeue

```ts
await client.dequeueMatchmaking()
```

## Handle Match Event

```ts
client.onMatchFound(async (match) => {
  // match.room, match.room_type, match.participants
  await client.joinOrCreate(match.room_type, { room: match.room })
})
```

Ticket TTL and disconnect cleanup are handled server-side.
